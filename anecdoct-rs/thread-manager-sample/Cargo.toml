[package]
name = "anecdoct-thread-manager-sample"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
# Keep this sample limited to a single Anecdoct workspace dependency.
# Add new Anecdoct surface area to `anecdoct-core-api` instead of depending on
# additional `anecdoct-*` crates here.
anecdoct-core-api = { workspace = true }
tracing = { workspace = true }
