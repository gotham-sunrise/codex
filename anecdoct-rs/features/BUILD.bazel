load("//:defs.bzl", "anecdoct_rust_crate")

anecdoct_rust_crate(
    name = "features",
    crate_name = "anecdoct_features",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ),
)
