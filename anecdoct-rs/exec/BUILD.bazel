load("//:defs.bzl", "anecdoct_rust_crate")

anecdoct_rust_crate(
    name = "exec",
    crate_name = "anecdoct_exec",
    test_shard_counts = {
        "exec-all-test": 8,
    },
    test_tags = ["no-sandbox"],
)
