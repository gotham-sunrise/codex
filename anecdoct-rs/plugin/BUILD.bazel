load("//:defs.bzl", "anecdoct_rust_crate")

anecdoct_rust_crate(
    name = "plugin",
    crate_name = "anecdoct_plugin",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ),
)
